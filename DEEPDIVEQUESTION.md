<h1 align="center">📚 Cyber Finance: 백엔드 설계 및 아키텍처 심화 과제</h1>
<p align="center">It's just for review and learning deeper about backend system</p>

본 과제는 현재 개발 중인 주식 관리 시스템의 기술적 한계를 분석하고, 실무적인 해결 방안을 모색하기 위한 서술형 문항입니다.

1. 동기적 지연과 스레드 관리

현재 StockController는 주식 하나당 약 1.1초의 대기 시간을 가집니다. 만약 관리 대상 주식이 50개로 늘어나고 동시 접속자가 100명이 되었을 때, 서버의 Thread Pool과 사용자 응답 속도에 어떤 영향을 미칠지 서술하세요.

내 생각 / 조사결과
1.1초 * 50개 * 100명 = 약 5500초 = 약 1.5시간의 대기 시간이 발생
심각한 수준의 서버 대기 상태가 발생한다.
그러나 그 코드에 대기 시간을 넣은것은 Alpha Vantage API의 Rate Limit을 피하기 위한 조치이고 테스트 코드이므로 실제 운영 환경에서는 필요하지 않을것이다.

Gemini의 해설
톰캣(Tomcat) 같은 서블릿 컨테이너는 보통 200개 내외의 스레드 풀을 가집니다. 한 요청이 스레드를 55초간 점유하면, 단 몇 명의 접속만으로 전체 스레드가 고갈되어 서버가 '먹통'이 됩니다. 실무에서는 **비동기 호출(WebClient)**이나 **캐싱(Redis)**을 통해 사용자가 대기하지 않게 설계합니다.

2. 보안: Password Hashing & Salt

사용자 비밀번호를 평문으로 저장하는 것은 보안상 치명적입니다. 단순히 SHA-256 해시를 사용하는 것보다 BCrypt와 같은 전용 알고리즘이 Rainbow Table Attack을 방어하는 데 왜 더 유리한지 논하세요.

내 생각 / 조사결과
SHA-256는 암호를 256bit로 해싱하는 것으로 비가역적 성질을 가진다.
하지만 암호->해시값이 고정함수로서 패턴을 알아낼 수 없지만 해시 값을 미리 계산한 테이블 값을 가지고 역추적을 통한 해킹이 가능하다.
BCrypt는 salting과 키 스트레칭을 이용한다.
salting이란 기존 해싱 함수에 추가적인 랜덤한 값을 추가하여 해시값을 변경하는 기법이다.
키 스트레칭은 해싱을 반복하여 해시값을 변경하는 기법으로 컴퓨터 성능 향상에 대처한다.
이는 Rainbow Table Attack을 방지한다.

Gemini의 해설
SHA-256은 설계 목적 자체가 '빠른 데이터 무결성 확인'이기에 공격자가 초당 수억 번의 대조를 할 수 있습니다. 반면 BCrypt는 의도적으로 연산 비용을 높여(Work Factor) 하드웨어 성능 향상에 대응하고, 같은 비밀번호라도 유저마다 다른 Salt를 부여해 보안성을 극대화합니다.

3. 데이터 무결성: Soft Delete vs Hard Delete

StockMaster 삭제 시 연관된 UserStock 데이터로 인해 발생하는 무결성 에러를 해결하기 위해, 데이터를 실제로 지우지 않는 Soft Delete 방식이 금융 시스템에서 왜 필수적인지 감사(Audit) 관점에서 설명하세요.

내 생각 / 조사결과
금융 시스템에서의 데이터는 매우 중요하며 유저가 관련 금융 정보의 소유권을 지녔다면 그 데이터는 바로 삭제되어서는 안된다. 따라서 Hard Delete 방식보다는 Soft Delete 방식을 통해 데이터를 삭제하더라도 복구를 할 수 있는 가능성을 열어두어야 한다.

Gemini의 해설
이를 **'감사 추적(Audit Trail)'**이라고 합니다. "누가, 언제, 왜 삭제했는가"는 금융권 규제(Compliance) 사항입니다. 또한 데이터가 물리적으로 사라지면 해당 데이터와 연결된 과거 통계나 리포트가 모두 깨지는 연쇄 장애가 발생하므로 Soft Delete는 기본 사양에 가깝습니다.

4. 계층 간 책임 분리 (Separation of Concerns)

환율 가공이나 외부 API 응답 처리 로직이 특정 계층에 집중되지 않을 경우 발생하는 문제점을 지적하고, Service 레이어가 비즈니스 로직의 중심이 되어야 하는 이유를 서술하세요.

내 생각 / 조사결과
외부 API 응답 처리 로직은 데이터를 불러오는 기능으로서 분산될 시 코드의 복잡도가 증가할 수 있다. Service 레이어라는 것은 유저의 요청에 따라 정해지는 계층으로 유저의 필요에 따라 비즈니스 로직이 결정된다.

Gemini의 해설
외부 API 응답 데이터 포맷이 변경될 때, Service 레이어에서 이를 가공해 주면 Controller나 Entity는 수정할 필요가 없습니다. 즉, **"변화의 영향 범위를 최소화"**하는 것이 계층 분리의 핵심 목적입니다.

5. 트랜잭션의 원자성 (Atomicity)

'주식 매수' 기능 구현 시 잔고 감소와 보유 수량 증가가 하나의 논리적 단위로 묶여야 합니다. 어느 한쪽만 성공했을 때 발생할 수 있는 데이터 오류와 이를 방지하기 위한 @Transactional의 역할을 기술하세요.

내 생각 / 조사결과
주식 매수와 같은 기능은 하나의 논리로 엮여야만 하며 아닐 시 큰 문제를 야기한다.
예를 들어 서버에서는 유저에게 돈을 빼갔다고 인식하지만 유저에게는 돈이 빠져나가지 않는 현상이 발생할 수도 있다. 그렇다면 이를 악용해서 서버에 돈을 빼갔다고 인식만 시키고 다시 돈을 돌려받으면 유저에 계좌에는 돈이 쌓이기만 하는 오류가 생길수도 있다.
이를 막기 위한 @Transactional의 역할은 데이터베이스 작업을 하나의 논리로 묶어, 예외 처리가 발생할 경우 롤백을 통해 데이터베이스 작업을 취소하는 역할을 한다.

Gemini의 해설
이를 'All or Nothing' 원칙이라고 합니다. 스프링은 Proxy 패턴을 통해 메서드 시작 시 트랜잭션을 열고, 예외 발생 시 DB 상태를 메서드 호출 전으로 되돌립니다. 금융 시스템에서 이 원자성이 깨지면 시스템 전체의 신뢰도가 붕괴됩니다.

6. 엔티티(Entity)와 DTO의 분리

DB 테이블과 1:1로 매핑되는 엔티티를 외부 API 응답으로 직접 노출했을 때 발생할 수 있는 보안 및 유지보수 측면의 위험성을 캡슐화 개념과 연결하여 설명하세요.

내 생각 / 조사결과


Gemini의 해설
보안(캡슐화): 엔티티는 DB 구조와 일치합니다. 유저 엔티티에 password나 socialId가 있을 때 이를 그대로 반환하면 보안 사고로 이어집니다. DTO는 보여주고 싶은 정보만 선별하여 보냅니다.

유지보수: 화면상의 필드명을 바꾸라고 할 때, DTO가 없다면 DB 컬럼명까지 바꿔야 합니다. DTO를 두면 DB 구조가 바뀌어도 API 스펙(JSON 형태)을 유지할 수 있어 시스템 유연성이 확보됩니다.

7. 장애 전파 방지 (Circuit Breaker)

외부 API(Alpha Vantage)가 다운되거나 응답이 비정상적으로 늦어질 때, 우리 서버 전체가 마비되는 것을 막기 위한 Fallback(대체값) 전략의 중요성을 서술하세요.

내 생각 / 조사결과
서버는 유지보수가 이뤄지더라도 계속 구동하도록 하는것이 제일 중요하기 때문에 외부 API의 다운에 대비한 Fallback 전략이 필요하다. 대채값은 주로 오류 문구, 이전 값들 재사용, 기본값 사용 등이 있다.

Gemini의 해설
이를 **'Graceful Degradation(우아한 성능 저하)'**라고 부릅니다. 핵심 기능(로그인 등)은 유지하되, 부가 기능(실시간 시세)만 잠시 제한함으로써 시스템 전체의 붕괴(Cascading Failure)를 막는 전략입니다.

8. 데이터 정규화와 조회 성능의 트레이드-오프

주식 정보를 별도 마스터 테이블로 분리하여 중복을 제거했지만, 조회 시 Join 비용이 발생합니다. 정규화 수준을 유지하면서도 조회 성능을 높일 수 있는 기술적 아이디어(예: 인덱싱, 반정규화 등)를 제안하세요.

내 생각 / 조사결과


Gemini의 해설
인덱싱(Indexing): DB에서 특정 컬럼을 기준으로 '목차'를 만드는 것입니다. Join의 기준이 되는 Foreign Key에 인덱스를 걸면 검색 속도가 기하급수적으로 빨라집니다.

반정규화(De-normalization): 성능을 위해 의도적으로 데이터 중복을 허용하는 것입니다. 예를 들어, 포트폴리오를 조회할 때 매번 주식 이름을 Join 하지 않도록 아예 포트폴리오 테이블에 주식 이름을 같이 저장해버리는 방식입니다. (저장 공간을 쓰고 속도를 얻는 선택)

9. 스케줄링과 배치(Batch) 처리

매번 실시간 API를 호출하는 대신, 특정 주기(예: 1분)마다 전 종목 시세를 업데이트하여 DB에 저장해두는 방식이 서버 부하와 Rate Limit 해결에 어떤 도움을 주는지 논하세요.

내 생각 / 조사결과
매번 실시간으로 API 호출 시 서버 부하가 증가한다. 따라서 특정 주기마다 API를 호출하여 DB에 저장해두고 일정 주기에 따라 주식 정보를 한번에 업데이트 하는 방식이 더 적절하다.

Gemini의 해설
유저 1,000명이 접속해도 API 호출은 서버가 1분에 딱 한 번만 수행하므로 Rate Limit 문제를 완벽히 해결하며 서버 비용을 절감합니다.

10. 환경 설정의 외부화 (Externalized Configuration)

API Key와 같은 민감 정보를 소스 코드에 하드코딩하지 않고 환경 변수나 별도 설정 파일로 관리해야 하는 이유를 보안과 CI/CD(배포 자동화) 관점에서 서술하세요.

내 생각 / 조사결과
별도 설정 파일로 분리 시 이점이 많다. 우선 소스 코드에 주요 정보가 들어있지 않아 보안에도 안전하며 배포 시 설정 파일 변경 시에도 소스 코드를 변경할 필요 없이 설정 파일만 간단히 수정하여 배포 가능하다.

Gemini의 해설
민감한 키가 깃허브에 노출되는 것을 막고, 하나의 빌드 결과물을 개발/운영 환경 등에서 설정만 바꿔 재사용할 수 있게 합니다.


============================================================
      🎓 [백엔드 개발 및 보안 심화] 단원 평가    
============================================================

[안내] 본 평가는 단순 암기가 아닌, '설계의 이유'를 묻는 10문제로 구성되었습니다.
코드를 직접 작성하는 것이 아니라, 원리를 서술하거나 논리를 전개해 보세요.

1. [DI/테스트] @Autowired를 필드에 직접 붙이는 '필드 주입'보다 '생성자 주입'을 권장하는 이유는 무엇인가요? 특히 Spring 컨테이너 없이 '순수 자바 코드'로 단위 테스트를 작성할 때의 관점에서 설명해 보세요.

내 생각 / 조사결과


Gemini의 해설


2. [보안/해싱] BCrypt는 해시를 생성할 때 'Cost Factor(Work Factor)'라는 개념을 사용합니다. 연산 속도가 빠를수록 좋은 일반적인 알고리즘과 달리, 암호 해싱에서 의도적으로 속도를 늦추는 이유는 무엇인가요?

내 생각 / 조사결과


Gemini의 해설


3. [자바/Optional] login 메서드에서 Optional의 .map()과 .orElse()를 체이닝했습니다. 만약 사용자가 존재하지 않아 Optional이 비어있을 때, .map() 내부의 로직은 실행되는지 여부와 최종 반환값이 결정되는 과정을 설명하세요.

내 생각 / 조사결과


Gemini의 해설


4. [Spring/트랜잭션] @Transactional이 붙은 메서드에서 'Checked Exception'(예: IOException)이 발생했을 때, 스프링의 기본 설정상 롤백(Rollback)이 발생하는지 설명하고 그 이유를 서술하세요.

내 생각 / 조사결과


Gemini의 해설


5. [아키텍처/SRP] 로그인을 처리할 때 DB 조회는 Repository, 로직은 Service, 응답은 Controller가 맡습니다. 만약 이 로직을 분리하지 않고 Controller에 다 넣었을 때 발생할 수 있는 '코드 재사용성' 측면의 문제를 설명하세요.

내 생각 / 조사결과


Gemini의 해설


6. [객체지향/DIP] UserService는 구체적인 BCryptPasswordEncoder가 아니라 PasswordEncoder 인터페이스에 의존합니다. 이렇게 설계했을 때, 암호화 알고리즘을 변경하더라도 UserService 코드를 수정하지 않아도 되는 원리를 설명하세요.

내 생각 / 조사결과


Gemini의 해설


7. [웹/세션 보안] 세션 ID가 담긴 쿠키에 'HttpOnly' 설정을 하지 않았을 때, 자바스크립트(XSS 공격 등)를 통해 발생할 수 있는 보안 위협은 무엇인가요?

내 생각 / 조사결과


Gemini의 해설


8. [빌드/Maven] Maven의 pom.xml에 의존성을 추가하면 '의존성 전이(Transitive Dependencies)'가 일어납니다. 내가 직접 추가하지 않은 라이브러리들이 어떻게 프로젝트에 포함되는지 그 과정을 설명하세요.

내 생각 / 조사결과


Gemini의 해설


9. [Spring/Bean] @Service 어노테이션을 붙인 클래스는 스프링이 기동될 때 어떻게 인스턴스로 생성되어 관리되나요? '컴포넌트 스캔(Component Scan)'의 관점에서 설명하세요.

내 생각 / 조사결과


Gemini의 해설


10. [API 설계] 회원가입(register) 후 성공 메시지 String 대신 User 객체(또는 DTO)를 반환하는 것이 클라이언트(프론트엔드)와의 협업 관점에서 왜 더 유리한가요?

내 생각 / 조사결과


Gemini의 해설


============================
프로젝트 마무리 최종 점검 10문항
============================

1. Spring Security 6 환경에서 수동 로그인 처리를 할 때, SecurityContextHolder에 인증 정보를 담는 것만으로 세션이 유지되지 않는 이유와 이를 해결하기 위한 SecurityContextRepository.saveContext의 역할을 설명하십시오.

내 생각 / 조사결과


Gemini의 해설


2. 사용자 비밀번호 저장 시 사용된 BCrypt 해시 알고리즘의 동작 원리와, 레인보우 테이블 공격을 방어하기 위한 솔팅(Salting) 및 키 스트레칭(Key Stretching)의 개념을 포함하여 서술하십시오.

내 생각 / 조사결과


Gemini의 해설


3. 포트폴리오 관리 시스템에서 주식 데이터를 식별하고 처리할 때, 종목명(Name) 대신 티커(Ticker)를 주요 식별자로 사용해야 하는 기술적 이유와 데이터 무결성 측면의 이점을 설명하십시오.

내 생각 / 조사결과


Gemini의 해설


4. Alpha Vantage 등 외부 API 연동 시 발생하는 호출 횟수 제한(Rate Limit) 문제에 대응하기 위해, 백엔드 서버에서 적용할 수 있는 효율적인 데이터 관리 전략(예: 캐싱, 스케줄링)에 대해 서술하십시오.

내 생각 / 조사결과


Gemini의 해설


5. Entity 객체를 프론트엔드 레이어에 직접 노출하지 않고 DTO(Data Transfer Object)를 별도로 정의하여 데이터를 전달해야 하는 이유와 시스템 유지보수 측면에서의 장점을 설명하십시오.

내 생각 / 조사결과


Gemini의 해설


6. 프로젝트 개발 과정에서 발생했던 자산 정보 표시 오류(NaN 등)의 원인을 백엔드 데이터 필드명과 프론트엔드 변수 매핑 관점에서 분석하고, 이를 방지하기 위한 협업 규칙을 설명하십시오.

내 생각 / 조사결과


Gemini의 해설


7. JPA를 활용하여 사용자(User), 주식 마스터(StockMaster), 보유 주식(Portfolio) 간의 연관 관계를 매핑할 때, 성능 최적화를 위한 로딩 전략(Lazy/Eager Loading) 적용 방안을 설명하십시오.

내 생각 / 조사결과


Gemini의 해설


8. REST API 설계 원칙에 따라 매수(Buy) 및 매도(Sell) 기능을 구현할 때, 적절한 HTTP 메서드 활용 방식과 예외 상황 발생 시 반환해야 하는 상태 코드(Status Code)에 대해 서술하십시오.

내 생각 / 조사결과


Gemini의 해설


9. Spring Boot의 3계층 아키텍처(Controller, Service, Repository)에서 각 계층이 담당해야 하는 구체적인 책임과 계층 간 데이터 흐름의 원칙을 설명하십시오.

내 생각 / 조사결과


Gemini의 해설


10. 금융 서비스 웹 애플리케이션 개발 시 보안 강화를 위해 CSRF(Cross-Site Request Forgery) 방어 설정이 필요한 이유와, 프로젝트에서 적용한 Spring Security 설정 방식을 설명하십시오.

내 생각 / 조사결과


Gemini의 해설
